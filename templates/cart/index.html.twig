{% extends 'base.html.twig' %}

{% block title %}Votre Panier{% endblock %}

{% block body %}
<head>
    <link rel="stylesheet" href="{{ asset('css/btn.css') }}">
    <link rel="stylesheet" href="{{ asset('css/cart.css') }}">


</head>
<div class="container">
<h1>Mon panier</h1>
</div>


    <table class="table mx-auto p4 mt6" style="width: 600px;">
    <div class="container">
        <thead>
            <tr>
                {# <th>Id_produit</th> #}
                {# <th>image</th> #}
                <th>titre</th>
                <th>description</th>
                <th>prix</th>
                <th>Quantite</th>
                <th>Total</th>
                <th>Suprrimer</th>
            </tr>
        </thead>
        <tbody>
            {% for monpanier in panier %}
            <tr>
                {# <td>{{ monpanier.produit.id}}</td> #}
                {# <td><img src="{{ asset('img/') ~monpanier.produit.imagename}}" class="w-25" ></td> #}
                <td>{{ monpanier.produit.nom }}</td>
                <td>{{ monpanier.produit.description }}</td>
                <td>{{ monpanier.produit.prix }}€</td>
                <td>
                    <a href="{{ path('app_cart_add', {'id': monpanier.produit.id}) }}">
                        <i class="fa fa-plus" aria-hidden="true"></i>
                    </a>
                    {{ monpanier.quantite }}
                    <a href="{{ path('app_cart_removequantite', {'id': monpanier.produit.id}) }}">
                        <i class="fa fa-minus" aria-hidden="true"></i>
                    </a>
                </td>
                <td>
                    {{monpanier.total}}€ 
                </td>
                <td>
                    <a href="{{ path('app_cart_remove', {'id': monpanier.produit.id}) }}">
                        <i class="fa fa-trash" aria-hidden="true"></i>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4 ">Votre panier vide</td>
            </tr>
        {% endfor %}
        </tbody>
    
    </table>

    <div class="container">
    <div class="row">
    <div class="basdepage col-12 ">
    <h2 class="h2cart col-12" >Montant Total : {{ totalcomplet }}€ </h2>
    </div>
        <div class="btn3">
            <a href="{{ path('app_commande') }}">
                <button type="button" class="btn col-4 table mx-auto p4 mt6" style="width: 600px;">
                    Commander
                </button>
            </a>
            <a href="{{ path('app_categories_index') }}">
                <button type="button" class="btn col-4 table mx-auto p4 mt6" style="width: 600px;">
                    Continuer mes achats
                </button>
            </a>
            <a href="{{ path('app_cart_clear') }}">
                <button type="button" class="btn col-4 table mx-auto p4 mt6" style="width: 600px;">
                    Vider le panier
                </button>
            </a>
        </div>
    
    </div>
    </div>
        {# </div>
    <div class="btnpanier">
    <a href="{{ path('app_categories_index') }}">
        <button type="button" class="btn btn- ">
            Continuer mes achats
        </button>
    </a>
    
    <p>ou</p>

    <a href="{{ path('app_commande') }}">
        <button type="button" class="btn btn-">
            Commander
        </button>
    </a>
    <br><br>
    <a href="{{ path('app_cart_clear') }}">
        <button type="button" class="btn  mb-5">
            Vider le panier
        </button>
    </a>
    </div> #}
{% endblock %}
{% block javascripts %}
            <script src="https://use.fontawesome.com/c33ed44af2.js"></script>
{% endblock %}